# Ideas System
> 建立长期可维护的创新点数据库。Idea ≠ Innovation，不是每个想法都值得做。

## Workflow
`Paper → Knowledge → Compare → Timeline → Research Gap → Candidate Idea → Evaluation → Validated Idea → Design Direction`

## Rules
- 新想法必须写入 Ideas/，不能只存上下文
- 每个Idea必须分类、不覆盖、不删除、持续演化
- 不要直接进入实验，必须经过评估
